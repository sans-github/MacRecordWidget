Status: Approved — EM
Approved: 2026-05-18

# Swift Engineer Issues List: Production Polish

## Issues

| # | Title | Labels | Description | Dependencies |
|---|-------|--------|-------------|--------------|
| SE-1 | Apply HIG-compliant button roles and semantic colors | `fe`, `ux` | In `MacRecordWidgetApp.swift`, apply `.buttonStyle(.bordered)` with `.tint(.red)` on the Stop button and `.tint(.accentColor)` on the Start button. Done when: both buttons render correct semantic colors in light mode, dark mode, and non-default accent color (AC-UI-1, AC-UI-2). | None |
| SE-2 | Audit control sizing and popover spacing | `fe`, `ux` | In `MacRecordWidgetApp.swift`, confirm `.bordered` button style satisfies macOS HIG click-target requirements, bump outer `.padding` to 16pt, frame width to 200pt, and inner button stack spacing to 8pt. Done when: no control touches the window edge and spacing matches the approved component spec; AC-UI-3 and AC-UI-6 are satisfied. | SE-1 (same file; avoids merge conflicts) |
| SE-3 | Confirm typography uses no hardcoded font sizes | `fe`, `ux` | Verify that no `.system(size: N)` calls exist in `MacRecordWidgetApp.swift`; if any exist, replace with semantic styles. Done when: a grep for `.system(size:` in the file returns no matches and AC-UI-4 is satisfied. | None |
| SE-4 | Bump deployment target to macOS 14.0 | `fe`, `debt` | Set `MACOSX_DEPLOYMENT_TARGET = 14.0` in both the Debug and Release build configurations in `MacRecordWidget.xcodeproj/project.pbxproj`. Also bump the GitHub Actions runner to `macos-14` or `macos-latest` in the workflow YAML. Done when: `xcodebuild` reports the deployment target as 14.0 for both configurations and CI passes (AC-SW-2). | None (must land before SE-5 to avoid linker errors) |
| SE-5 | Migrate RecordingManager to @Observable | `fe`, `debt` | Replace `ObservableObject` / `@Published` / `@StateObject` with `@Observable` / `@State` in `RecordingManager.swift` and `MacRecordWidgetApp.swift`. Add `isInFlight: Bool` stored property. Done when: no `ObservableObject`, `@StateObject`, `@ObservedObject`, or `@Published` symbols remain in either file; app builds clean with zero warnings; AC-SW-1 is satisfied. | SE-4 (macOS 14 target required for @Observable) |
| SE-6 | Replace DispatchQueue background calls with async/await | `fe`, `debt` | In `RecordingManager.swift`, replace `DispatchQueue.global()` + `Thread.sleep` with `Task { }` + `try await Task.sleep(for:)`. Retain the single `DispatchQueue.main.asyncAfter` in the `orderOut` dismissal block per CLAUDE.md. Done when: no `DispatchQueue.global()` or `Thread.sleep` calls remain; `startRecording()` and `stopRecording()` are declared `async throws`; AC-SW-3 is satisfied. | SE-5 (@Observable migration must land first to keep changes reviewable) |
| SE-7 | Add typed RecordingError enum and propagate errors to caller | `fe`, `debt` | Add `RecordingError: Error, LocalizedError` with cases `shortcutLaunchFailed(reason: String)` and `accessibilityDenied`. Update `startRecording()` and `stopRecording()` to throw on `NSWorkspace.open` returning false. Move `isRecording = true` to after the successful open guard. Done when: both functions throw typed errors on failure; `isRecording` is never set before a successful open; AC-SW-4 and AC-SW-7 are satisfied. | SE-6 (signatures become async throws in this step) |
| SE-8 | Handle Accessibility denial from osascript and surface NSAlert | `fe`, `bug` | In `launchPhotoBooth()`, detect non-zero `terminationStatus` from the `osascript` Process, set `isRecording = false`, and throw `RecordingError.accessibilityDenied`. In `MacRecordWidgetApp`, add `presentAlert(for:)` that shows an `NSAlert` for both error cases. Add `isInFlight` guard at the top of both recording functions with `defer { isInFlight = false }`. Done when: denying Accessibility permission during a video-mode start shows the correct alert, leaves `isRecording == false`, and re-enables the Start button; rapid double-taps produce only one shortcut invocation; AC-SW-5, AC-SW-6, and AC-SW-8 are satisfied. Add the four-line inline comment to both `orderOut` dismissal sites referencing CLAUDE.md. | SE-7 (RecordingError enum required) |
